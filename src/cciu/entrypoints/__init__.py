"""Command-line entrypoints for the ``cciu`` package.

Each module in this subpackage exposes an ``add_arguments`` helper and a
``main`` function that is registered as a subcommand of the ``cciu`` CLI.
"""
